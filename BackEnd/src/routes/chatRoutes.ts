
// import { Router } from 'express';
// import { sendMessage, getMessages, createChatRoom, emitRoomForChat } from '../controller/ChatController';

// const router = Router();

// router.post('/send', sendMessage);
// router.get('/:chatRoomId', getMessages);
// router.post('/chat-room', createChatRoom);
// router.get('/chat/:chatRoomId', getMessages);
// router.post('/chat-room/:chatRoomId/emit', emitRoomForChat);


// export default router;